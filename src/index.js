import React from 'react';
import ReactDOM from 'react-dom/client';
import AppRenderContainer from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<AppRenderContainer />);
